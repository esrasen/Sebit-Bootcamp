import { HoroscopeCalculatorPipe } from './horoscope-calculator.pipe';

describe('HoroscopeCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new HoroscopeCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
